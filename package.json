{
  "name": "korona-visual-back",
  "version": "1.0.0",
  "description": "express server",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "watch": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../korona-visual && npm run build --prod && cp -r build ../korona-visual-back"
  
  },
  "author": "Petri Palmu",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.19.2",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "memoizee": "^0.4.14",
    "memory-cache": "^0.2.0",
    "redis": "^3.1.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
